/**
 * APP — mounts widgets, binds keys, owns the loop.
 * WHO WRITES THIS: you. Wiring, no economics.
 */

/** Keyboard: space pause, +/- speed, w why, e events, r restart same seed. */
export function boot() {
  throw new Error('app.boot: not implemented');
}
